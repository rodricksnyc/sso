import type { FC } from 'react';
import type { ChartLayoutProps, VisWrapperProps, SupportedChartTypes, SDKRecord, Fields, CAll, Totals } from '@looker/visualizations-adapters';
export interface VisualizationProps extends VisWrapperProps, ChartLayoutProps {
    data?: SDKRecord[];
    fields?: Fields;
    config?: CAll;
    totals?: Totals;
}
export declare const chartComponentMap: Record<keyof SupportedChartTypes | 'default', FC<any>>;
export declare const Visualization: FC<VisualizationProps>;
