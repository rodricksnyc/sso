"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getXAxisFormat = exports.getYAxisFormat = void 0;

require("core-js/modules/es.object.values.js");

var _visualizationsAdapters = require("@looker/visualizations-adapters");

const getYAxisFormat = config => {
  const _config$series = config.series,
        series = _config$series === void 0 ? {} : _config$series,
        _config$positioning = config.positioning,
        positioning = _config$positioning === void 0 ? '' : _config$positioning;
  const isPercent = positioning === 'percent';
  const isSingleSeries = !!(series.length === 1 || Object.keys(series).length === 1);

  if (isSingleSeries) {
    const valueFormat = Array.isArray(series) ? series[0].value_format : Object.values(series)[0].value_format;
    return valueFormat;
  } else {
    return isPercent ? _visualizationsAdapters.DEFAULT_STRING_FORMAT_PERCENT : _visualizationsAdapters.DEFAULT_STRING_FORMAT;
  }
};

exports.getYAxisFormat = getYAxisFormat;

const getXAxisFormat = fields => {
  if (typeof fields === 'undefined') return '';
  const _fields$dimensions = fields.dimensions,
        dimensions = _fields$dimensions === void 0 ? '' : _fields$dimensions;

  if (dimensions && dimensions.length === 1) {
    const value_format = dimensions[0].value_format;
    return value_format;
  } else {
    return _visualizationsAdapters.DEFAULT_STRING_FORMAT;
  }
};

exports.getXAxisFormat = getXAxisFormat;
//# sourceMappingURL=getAxisFormat.js.map