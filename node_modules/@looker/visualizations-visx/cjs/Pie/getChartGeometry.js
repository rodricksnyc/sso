"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getChartGeometry = void 0;

var _pieConstants = require("./pieConstants");

var _getConnectorLength = require("./getConnectorLength");

const getChartGeometry = ({
  width,
  height,
  labelWidth,
  legendType = 'legend'
}) => {
  const minChartSize = legendType === 'legend' ? 50 : 350;
  const maxConnectorLength = (0, _getConnectorLength.getConnectorLength)(0, 1) * 2;
  const maxDimension = Math.max(width, height, minChartSize);
  const hoverMargin = Math.ceil(maxDimension - maxDimension / _pieConstants.PIE_SLICE_ZOOM);
  const canvasCenter = (maxDimension - hoverMargin) / 2;
  const padding = legendType === 'labels' ? Math.max(labelWidth, maxConnectorLength) : 0;
  const outerRadius = canvasCenter - hoverMargin - padding;
  return {
    canvasW: maxDimension,
    canvasH: maxDimension - padding,
    pieCenterX: canvasCenter,
    pieCenterY: canvasCenter - padding / 2,
    outerRadius
  };
};

exports.getChartGeometry = getChartGeometry;
//# sourceMappingURL=getChartGeometry.js.map