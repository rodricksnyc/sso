import "core-js/modules/es.object.values.js";
import "core-js/modules/es.array.map.js";
import { useContext } from 'react';
import { ThemeContext } from 'styled-components';
import isArray from 'lodash/isArray';
import compact from 'lodash/compact';
import { buildChartTheme } from '@visx/xychart';
export var useChartTheme = function useChartTheme(series) {
  var theme = useContext(ThemeContext);
  var seriesList = series && isArray(series) ? series : Object.values(series || {});
  var scheme = compact(seriesList.map(function (s) {
    return s.visible && s.color;
  }));
  var chartTheme = buildChartTheme({
    backgroundColor: theme.colors.background,
    colors: scheme.length ? scheme : [theme.colors.key],
    gridColor: theme.colors.ui2,
    gridColorDark: theme.colors.text5,
    svgLabelBig: {
      fill: theme.colors.text5
    },
    svgLabelSmall: {
      fill: theme.colors.text5
    },
    tickLength: 8
  });
  return chartTheme;
};
//# sourceMappingURL=useChartTheme.js.map