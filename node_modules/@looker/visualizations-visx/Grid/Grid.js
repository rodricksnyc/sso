import React from 'react';
import { Grid as VisXGrid } from '@visx/xychart';
import every from 'lodash/every';
export var Grid = function Grid(_ref) {
  var config = _ref.config;
  var x_axis = (config === null || config === void 0 ? void 0 : config.x_axis) || [];
  var y_axis = (config === null || config === void 0 ? void 0 : config.y_axis) || [];
  var showGridX = every(x_axis, ['gridlines', true]);
  var showGridY = every(y_axis, ['gridlines', true]);
  return React.createElement(VisXGrid, {
    rows: showGridY,
    columns: showGridX
  });
};
//# sourceMappingURL=Grid.js.map