import React, { useContext } from 'react';
import { ThemeContext } from 'styled-components';
import { Marker as VisxMarker } from '@visx/marker';
import { Glyph } from '../Glyph';
import { getMarkerFill, getDefaultGlyphSize } from '../utils';
export var Marker = function Marker(_ref) {
  var series = _ref.series,
      id = _ref.id;
  var theme = useContext(ThemeContext);
  var _series$line_width = series.line_width,
      line_width = _series$line_width === void 0 ? 1 : _series$line_width;
  var fill = getMarkerFill(series, theme);
  var size = getDefaultGlyphSize(line_width);
  var top = size / 2;
  var left = size / 2;
  return React.createElement(VisxMarker, {
    id: "".concat(id),
    fill: fill,
    markerWidth: size,
    markerHeight: size,
    refX: left,
    refY: top
  }, React.createElement(Glyph, {
    series: series,
    top: top,
    left: left,
    size: size,
    fill: fill
  }));
};
//# sourceMappingURL=Marker.js.map