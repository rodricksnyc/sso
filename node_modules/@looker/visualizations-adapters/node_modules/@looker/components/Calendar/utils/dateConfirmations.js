import "core-js/modules/es.date.to-string.js";
import { isSameYear, getMonth } from 'date-fns';
export var confirmToday = function confirmToday(i) {
  return getMonth(new Date()) === i;
};
export var isThisMonth = function isThisMonth(date, monthNumber, selectedMonth) {
  return selectedMonth && isSameYear(selectedMonth, date) && getMonth(selectedMonth) === monthNumber;
};
//# sourceMappingURL=dateConfirmations.js.map