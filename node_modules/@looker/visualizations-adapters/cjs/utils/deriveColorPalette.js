"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.deriveColorPalette = void 0;

require("core-js/modules/es.array.flat-map.js");

require("core-js/modules/es.array.unscopables.flat-map.js");

require("core-js/modules/es.array.map.js");

var _d3Hsv = require("d3-hsv");

var _utils = require("../utils");

const colorTransformations = [{
  h: 0,
  s: 0,
  v: 0
}, {
  h: 45,
  s: -0.05,
  v: 0
}, {
  h: 90,
  s: 0,
  v: 0
}, {
  h: -60,
  s: 0,
  v: 0
}, {
  h: -90,
  s: 0,
  v: 0
}, {
  h: 180,
  s: 0,
  v: 0
}, {
  h: 0,
  s: 0.15,
  v: 0.15
}, {
  h: 0,
  s: 0.15,
  v: -0.25
}, {
  h: 0,
  s: -0.25,
  v: -0.25
}, {
  h: 180,
  s: -0.25,
  v: -0.25
}];

const deriveColorPalette = (baseColors = _utils.DEFAULT_SERIES_COLORS) => {
  return colorTransformations.flatMap(t => {
    const hueDiff = t.h,
          satDiff = t.s,
          valDiff = t.v;
    return baseColors.map(color => {
      const _hsv = (0, _d3Hsv.hsv)(color),
            h = _hsv.h,
            s = _hsv.s,
            v = _hsv.v;

      const newHue = (h + hueDiff) % 360;
      const newSat = Math.max(Math.min(s + satDiff, 1), 0);
      const newVal = Math.max(Math.min(v + valDiff, 1), 0);
      return (0, _d3Hsv.hsv)(newHue, newSat, newVal).hex();
    });
  });
};

exports.deriveColorPalette = deriveColorPalette;
//# sourceMappingURL=deriveColorPalette.js.map