"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.normalizePivotSeriesKeys = void 0;

require("core-js/modules/es.array.map.js");

require("core-js/modules/es.object.entries.js");

require("core-js/modules/es.array.iterator.js");

require("core-js/modules/es.array.last-index-of.js");

require("core-js/modules/es.regexp.exec.js");

require("core-js/modules/es.string.replace.js");

require("core-js/modules/es.object.from-entries.js");

var _ = require(".");

const SEPARATOR = ' - ';

const normalizePivotSeriesKeys = rawConfig => {
  const entries = Object.entries(rawConfig).map(([key, val]) => {
    const pivotPos = key.lastIndexOf(SEPARATOR);
    const measureName = key.substring(pivotPos + SEPARATOR.length);
    const pivotValue = key.substring(0, pivotPos);
    const formattedKey = pivotPos > -1 ? (0, _.buildPivotMeasureName)({
      measureName,
      pivotValue: pivotValue.replace(/Row Total/, '$$$$$$_row_total_$$$$$$')
    }) : key;
    return [formattedKey, val];
  });
  return Object.fromEntries(entries);
};

exports.normalizePivotSeriesKeys = normalizePivotSeriesKeys;
//# sourceMappingURL=normalizePivotSeriesKeys.js.map