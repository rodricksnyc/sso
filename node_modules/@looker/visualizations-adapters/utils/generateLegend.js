import "core-js/modules/es.array.join.js";
import map from 'lodash/map';
import get from 'lodash/get';
var ENDASH = "\u2013";
export var generateLegend = function generateLegend() {
  var fields = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {
    dimensions: [],
    measures: []
  };
  var config = arguments.length > 1 ? arguments[1] : undefined;
  var defaultDimensionLabel = map(fields.dimensions, function (d) {
    return d.label;
  }).join(" ".concat(ENDASH, " "));
  var defaultMeasureLabel = map(fields.measures, function (m) {
    return m.label;
  }).join(" ".concat(ENDASH, " "));
  return {
    dimension: get(config, 'x_axis_label', defaultDimensionLabel),
    measure: get(config, 'y_axes[0].label', defaultMeasureLabel)
  };
};
//# sourceMappingURL=generateLegend.js.map