import "core-js/modules/es.array.flat-map.js";
import "core-js/modules/es.array.unscopables.flat-map.js";
import "core-js/modules/es.array.map.js";
import { hsv } from 'd3-hsv';
import { DEFAULT_SERIES_COLORS } from '../utils';
var colorTransformations = [{
  h: 0,
  s: 0,
  v: 0
}, {
  h: 45,
  s: -0.05,
  v: 0
}, {
  h: 90,
  s: 0,
  v: 0
}, {
  h: -60,
  s: 0,
  v: 0
}, {
  h: -90,
  s: 0,
  v: 0
}, {
  h: 180,
  s: 0,
  v: 0
}, {
  h: 0,
  s: 0.15,
  v: 0.15
}, {
  h: 0,
  s: 0.15,
  v: -0.25
}, {
  h: 0,
  s: -0.25,
  v: -0.25
}, {
  h: 180,
  s: -0.25,
  v: -0.25
}];
export var deriveColorPalette = function deriveColorPalette() {
  var baseColors = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : DEFAULT_SERIES_COLORS;
  return colorTransformations.flatMap(function (t) {
    var hueDiff = t.h,
        satDiff = t.s,
        valDiff = t.v;
    return baseColors.map(function (color) {
      var _hsv = hsv(color),
          h = _hsv.h,
          s = _hsv.s,
          v = _hsv.v;

      var newHue = (h + hueDiff) % 360;
      var newSat = Math.max(Math.min(s + satDiff, 1), 0);
      var newVal = Math.max(Math.min(v + valDiff, 1), 0);
      return hsv(newHue, newSat, newVal).hex();
    });
  });
};
//# sourceMappingURL=deriveColorPalette.js.map