/// <reference types="react" />
import type { LocaleUtils } from 'react-day-picker';
import type { CalendarSize } from './calendar-size';
export declare const currentYear: number;
export declare const fromMonth: Date;
export declare const toMonth: Date;
declare type NavCB = (date: Date) => void;
declare type YearMonthFormProps = {
    selectMonth?: boolean;
    date?: Date;
    disabled?: boolean;
    localeUtils?: LocaleUtils;
    onChange?: NavCB;
    onNowClick?: NavCB;
    size?: CalendarSize;
};
export declare const YearMonthForm: ({ selectMonth, date, disabled, localeUtils, onChange, onNowClick, size, }: YearMonthFormProps) => JSX.Element;
export {};
