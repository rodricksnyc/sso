import "core-js/modules/es.array.concat.js";
import { LocaleUtils } from 'react-day-picker';
export var formatMonthTitle = function formatMonthTitle(locale) {
  return function (month) {
    if (locale) {
      var _locale$localize;

      return "".concat((_locale$localize = locale.localize) === null || _locale$localize === void 0 ? void 0 : _locale$localize.month(month.getMonth()), " ").concat(month.getFullYear());
    } else {
      return LocaleUtils.formatMonthTitle(month);
    }
  };
};
//# sourceMappingURL=formatMonthTitle.js.map