import any from 'lodash/fp/any';
import allPass from 'lodash/fp/allPass';
import flow from 'lodash/fp/flow';
import { TYPE_USER_ATTRIBUTE } from '../types';
import { treeToList } from './tree';

var isUserAttributeNode = function isUserAttributeNode(_ref) {
  var type = _ref.type;
  return type === TYPE_USER_ATTRIBUTE;
};

var hasNoAttributeValue = function hasNoAttributeValue(_ref2) {
  var attributeValue = _ref2.attributeValue;
  return !attributeValue;
};

export var hasUserAttributeNodeWithoutValue = flow([treeToList, any(allPass([isUserAttributeNode, hasNoAttributeValue]))]);
//# sourceMappingURL=has_user_attribute_node_without_value.js.map