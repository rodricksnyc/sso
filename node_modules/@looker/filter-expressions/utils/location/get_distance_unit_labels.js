import "core-js/modules/es.function.bind.js";
import i18next from 'i18next';
export var getDistanceUnitLabels = function getDistanceUnitLabels(unit) {
  var t = i18next.t.bind(i18next);

  switch (unit) {
    case 'feet':
      return t('feet', {
        ns: 'get_distance_unit_labels'
      });

    case 'kilometers':
      return t('kilometers', {
        ns: 'get_distance_unit_labels'
      });

    case 'meters':
      return t('meters', {
        ns: 'get_distance_unit_labels'
      });

    case 'miles':
      return t('miles', {
        ns: 'get_distance_unit_labels'
      });
  }

  return '';
};
//# sourceMappingURL=get_distance_unit_labels.js.map