import "core-js/modules/es.array.concat.js";
import { inorderTraversal } from './inorder_traversal';
export var treeToList = function treeToList(root) {
  var orItems = [];
  var andItems = [];
  inorderTraversal(root, function (node) {
    var item = node;

    if (item.type !== ',') {
      ;
      (item.is ? orItems : andItems).push(item);
    }
  });
  return [].concat(orItems, andItems);
};
//# sourceMappingURL=tree_to_list.js.map