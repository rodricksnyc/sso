import applyId from '../transform/utils/apply_id_to_ast';
export var addNode = function addNode(root, newNode) {
  var workingRoot = root;

  if (!workingRoot.right) {
    var newRoot = {
      type: ',',
      left: workingRoot,
      right: newNode
    };
    return applyId(newRoot);
  }

  var parent = workingRoot;
  var pointer = workingRoot;

  while (pointer.right) {
    parent = pointer;
    pointer = pointer.right;
  }

  parent.right = {
    type: ',',
    left: pointer,
    right: newNode
  };
  return applyId(workingRoot);
};
//# sourceMappingURL=add_node.js.map