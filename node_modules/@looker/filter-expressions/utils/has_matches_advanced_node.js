import "core-js/modules/es.array.index-of.js";
import any from 'lodash/fp/any';
import flow from 'lodash/fp/flow';
import { treeToList } from './tree';

var isMatchesAdvancedItem = function isMatchesAdvancedItem(subTypes) {
  return function (_ref) {
    var type = _ref.type;
    return type === 'matchesAdvanced' || subTypes.indexOf(type) === -1;
  };
};

export var hasMatchesAdvancedNode = function hasMatchesAdvancedNode(subTypes) {
  return flow([treeToList, any(isMatchesAdvancedItem(subTypes))]);
};
//# sourceMappingURL=has_matches_advanced_node.js.map