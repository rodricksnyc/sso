import { get24HourTime } from './format_time';
var hoursInDay = 24;
export var generateTimes = function generateTimes() {
  var timeValues = [];

  for (var i = 0; i < hoursInDay; i++) {
    timeValues.push(get24HourTime({
      hour: i
    }));
  }

  return timeValues;
};
//# sourceMappingURL=generate_times.js.map